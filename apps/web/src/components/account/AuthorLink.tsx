import Link from "next/link";

interface AuthorLinkProps {
  authorId: Number;
  authorName: string;
}

export default function AuthorLink({ authorId, authorName }: AuthorLinkProps) {
  return (
    <div>
        
    </div>
  );
}
